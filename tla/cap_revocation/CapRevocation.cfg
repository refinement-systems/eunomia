SPECIFICATION Spec

CONSTANTS
    CapIds = {c0, c1, c2, c3}
    Procs  = {p0, p1}
    Channels = {ch0}
    NULL = "null"

INVARIANT TypeOK
INVARIANT NoDoubleOwnership

\* Uncomment when Revoke action is added:
\* INVARIANT RevocationComplete
